# Asahi Installer Metadata Shape Analysis

## Overview
- **Top-level keys**: `description`, `version`, `os_list`
- **`os_list` Entry keys**: `name`, `id`, `package`, `checksum`, `size`, `manifest`, `compatibility`
- **URL Path model**: Employs package relative paths (e.g. `packages/asahi-desktop.tar.zst`).
