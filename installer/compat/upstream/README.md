# Upstream Metadata Snapshots

This folder manages static JSON snapshots representing upstream installer data shapes.

- `asahi-installer-data.snapshot.json`: Mock layout for Asahi OS selection structures.
- `alarm-installer-data.snapshot.json`: Mock layout for Arch Linux ARM specific bootstrap definitions.
