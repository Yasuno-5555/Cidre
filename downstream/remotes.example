# Example Remote Layout

For a forked downstream repository:

```sh
git remote rename origin upstream
git remote add origin git@github.com:Yasuno-5555/jackrose-alarm-installer.git
git fetch upstream
git fetch origin
```

Suggested branch names:

- `jackrose/main`
- `jackrose/image-prototype`
- `jackrose/installer-entry`
