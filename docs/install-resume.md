# Install Resume Model

If the installation process is interrupted or fails, the system can determine where it was using `install-state.json`.

## Rerun Commands
- macOS: `./install-macos --resume`
- Root preinstall: `./preinstall --resume`
- User setup: `./install --resume`
- Recovery: `cidre-recovery install-resume`
