# Uninstall Resume

- **Resume Model**: State storage maintains the active stage and execution markers.
- **Safe Commands**: State manipulation tools are non-destructive and can be run to inspect parameters.
- **Blocked States**: Errors or safety checks trigger a `blocked` status, locking down the next steps until resolved or reset.
