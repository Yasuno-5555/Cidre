#!/bin/sh
# preinstall: Root-level entrypoint script to launch Jackrose preinstall helper
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec "${SCRIPT_DIR}/scripts/jackrose-preinstall" "$@"
