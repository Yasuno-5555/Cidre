# Jackrose Bootstrap Component

This component area is reserved for the Post-ALARM Bootstrap flow.

The active runtime entrypoint is `scripts/jackrose-bootstrap`.

Planned step families:

- environment-check
- user-check
- network-check
- pacman-check
- repo-setup
- package-install
- config-deploy
- input-setup
- session-setup
- firstboot-enable
- doctor-run
