Jackrose v1.0 session package

This package owns the public Jackrose login entry and session launcher.

It depends on `niri-jackrose` as the compositor binary started by the standard Jackrose session path.
