# Maintainer: Yasuno <yasuno@example.invalid>

pkgname=cidre-preinstall
pkgver=1.0.0
pkgrel=1
pkgdesc='Cidre root-phase preinstall dashboard and setup wizard'
url='https://github.com/Yasuno-5555/Cidre'
arch=('any')
license=('custom')
depends=('bash' 'pacman' 'sudo')
optdepends=(
  'dialog: preferred TUI backend for guided preinstall'
  'libnewt: whiptail backend for guided preinstall'
)
source=('cidre-preinstall')
sha256sums=('SKIP')

package() {
  install -Dm755 "${srcdir}/cidre-preinstall" "${pkgdir}/usr/bin/cidre-preinstall"
}
